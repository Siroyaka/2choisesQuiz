import IPages from './interface';

import { pageData as c_pageData } from './choise_quiz';
import { pageData as two_pageData } from './twochoise';

export const pageData = [...c_pageData, ...two_pageData];